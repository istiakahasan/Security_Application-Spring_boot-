spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id:  ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
          github:
            client-id: fsfsfsdfsd
            client-secret: fwewgergrrgasa
jwt:
  secretKey: ${JWT_SECRET_KEY}